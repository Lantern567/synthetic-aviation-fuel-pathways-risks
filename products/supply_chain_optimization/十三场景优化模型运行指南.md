# 十三场景SAF供应链优化模型运行指南
# 13 Scenarios SAF Supply Chain Optimization Model Execution Guide

本文档详细说明如何使用命令行启动所有13个SAF供应链优化场景模型。

---

## 目录 | Table of Contents

1. [使用说明](#使用说明)
2. [绿氢场景 (7个)](#绿氢场景)
3. [副产氢场景 (6个)](#副产氢场景)
4. [通用参数说明](#通用参数说明)
5. [常见问题](#常见问题)

---

## 使用说明

### 基本格式

```bash
nohup python <脚本路径> [参数] > <日志文件> 2>&1 &
```

### 重要提示

1. **确保Gurobi许可证配置正确**：`/home/ljt/gurobi.lic`
2. **确保conda环境激活**：`conda activate green_methanol_for_port_transportation`
3. **后台运行监控**：使用 `tail -f <日志文件>` 查看实时进度
4. **进程管理**：使用 `ps aux | grep python` 查看运行中的进程
5. **终止进程**：使用 `kill <进程ID>` 终止指定进程

---

## 绿氢场景

绿氢场景使用可再生能源（风能、太阳能）通过电解水制氢。

### 1. 煤制氢+绿氢两步法 | Coal Gasification CO₂ + Green H2 Two-Step

**场景说明**：使用煤气化提供CO₂，可再生能源电解水制氢（绿氢），两步法合成SAF（甲醇路线）

**配置文件**：`products/supply_chain_optimization/coal_hydrogen_saf_optimization/data/CoalHydrogenSAFOptimizer_config.yaml`

**技术路线**：
- CO₂来源：煤气化副产CO₂（化石碳）
- 氢气来源：风能/太阳能电解水（绿氢）
- 合成工艺：CO₂ + H₂ → 甲醇 → SAF

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/coal_hydrogen_saf_optimization/src/runner/unified_optimizer_runner.py \
  --process-type two_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > coal_green_h2_two_step.log 2>&1 &
```

**输出目录**：`coal_hydrogen_saf_optimization/results/`

**聚类文件**：`clustering_results.json` (绿氢聚类)

**重要说明**：
- 此场景使用绿氢（可再生能源电解），CO₂利用不计入负排放（因CO₂来自化石燃料）
- 与副产氢场景使用不同的配置文件（场景8使用 `shared/data/CoalByproductHydrogenSAFOptimizer_config.yaml`）
- 碳排放参数：`enable_co2_utilization_credit: false`（符合国际碳核算标准）
- **2025-11-23修复**：配置文件路径已从 `shared/data/` 修正为 `products/.../data/`

---

### 2. DAC制氢两步法 | DAC Hydrogen Two-Step

**场景说明**：使用DAC（直接空气捕获）技术捕获CO₂，电解水制氢，两步法合成SAF

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/dac_hydrogen_saf_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process two_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > dac_two_step.log 2>&1 &
```

**输出目录**：`dac_hydrogen_saf_supply_chain_optimization/results/two_step/`

---

### 3. DAC制氢一步法 | DAC Hydrogen One-Step

**场景说明**：使用DAC捕获CO₂，一步法直接合成SAF（Fischer-Tropsch工艺）

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/dac_hydrogen_saf_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process one_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > dac_one_step.log 2>&1 &
```

**输出目录**：`dac_hydrogen_saf_supply_chain_optimization/results/one_step/`

---

### 4. 天然气两步法 | Natural Gas Two-Step

**场景说明**：使用天然气重整制氢，两步法合成SAF

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/natural_gas_supply_chain_optimization/run_optimization.py \
  > ng_two_step.log 2>&1 &
```

**输出目录**：`natural_gas_supply_chain_optimization/results/`

**注意**：此脚本不支持命令行参数，配置在代码中硬编码。

---

### 5. 天然气一步法 | Natural Gas One-Step

**场景说明**：使用天然气一步法直接合成SAF（Fischer-Tropsch工艺）

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/natural_gas_supply_chain_optimization/src/core/natural_gas_optimization_model_one_step.py \
  > ng_one_step.log 2>&1 &
```

**输出目录**：`natural_gas_supply_chain_optimization/results/ft_one_step/`

**注意**：此脚本不支持命令行参数，直接运行核心模型文件。

---

### 6. 绿氢+工业捕获CO₂两步法 | Green H2 + Industrial CO2 Two-Step

**场景说明**：使用可再生能源电解水制氢，捕获工业排放CO₂，两步法合成SAF

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/green_hydrogen_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process two_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > green_h2_two_step.log 2>&1 &
```

**输出目录**：`green_hydrogen_supply_chain_optimization/results/two_step/`

---

### 7. 绿氢+工业捕获CO₂一步法 | Green H2 + Industrial CO2 One-Step

**场景说明**：使用可再生能源电解水制氢，捕获工业排放CO₂，一步法合成SAF

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/green_hydrogen_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process one_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > green_h2_one_step.log 2>&1 &
```

**输出目录**：`green_hydrogen_supply_chain_optimization/results/one_step/`

---

## 副产氢场景

副产氢场景利用钢铁厂和炼油厂副产的氢气，降低制氢成本。

### 8. 副产氢+煤气化CO₂两步法 | Byproduct H2 + Coal Gasification CO₂ Two-Step

**场景说明**：结合钢铁厂/炼油厂副产氢和煤气化CO₂，两步法合成SAF（甲醇路线）

**配置文件**：`CoalByproductHydrogenSAFOptimizer_config.yaml`

**技术路线**：
- CO₂来源：煤气化副产CO₂（化石碳）
- 氢气来源：钢铁厂/炼油厂副产氢（工业副产）
- 合成工艺：CO₂ + H₂ → 甲醇 → SAF

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/coal_hydrogen_saf_optimization/src/runner/unified_optimizer_runner.py \
  --process-type byproduct_two_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > byproduct_coal_two_step.log 2>&1 &
```

**输出目录**：`coal_hydrogen_saf_optimization/results/byproduct_hydrogen/`

**聚类文件**：`byproduct_clustering_results.json` (副产氢聚类，自动检测并重命名)

**重要说明**：
- 该模块会自动检测副产氢数据（位置名称包含 `steel_`、`refinery_` 或 `byproduct`）
- 检测到副产氢后，聚类文件自动命名为 `byproduct_clustering_results.json`
- 与绿氢场景使用不同的配置文件（场景1使用 `CoalHydrogenSAFOptimizer_config.yaml`）
- 副产氢成本更低，但供应量有限

---

### 9. 副产氢+DAC两步法 | Byproduct H2 + DAC Two-Step

**场景说明**：结合副产氢和DAC捕获CO₂，两步法合成SAF

**配置文件**：`DACByproductHydrogenSAFOptimizer_config_two_step.yaml`

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/dac_hydrogen_saf_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process byproduct_two_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > byproduct_dac_two_step.log 2>&1 &
```

**输出目录**：`dac_hydrogen_saf_supply_chain_optimization/results/byproduct_hydrogen/two_step/`

**聚类文件**：`byproduct_clustering_results.json` (自动检测副产氢并重命名)

---

### 10. 副产氢+DAC一步法 | Byproduct H2 + DAC One-Step

**场景说明**：结合副产氢和DAC捕获CO₂，一步法合成SAF

**配置文件**：`DACByproductHydrogenSAFOptimizer_config_one_step.yaml`

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/dac_hydrogen_saf_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process byproduct_one_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > byproduct_dac_one_step.log 2>&1 &
```

**输出目录**：`dac_hydrogen_saf_supply_chain_optimization/results/byproduct_hydrogen/one_step/`

**聚类文件**：`byproduct_clustering_results.json` (自动检测副产氢并重命名)

---

### 11. 副产氢+天然气两步法 | Byproduct H2 + Natural Gas Two-Step

**场景说明**：结合副产氢和天然气重整，两步法合成SAF

**配置文件**：`NaturalGasByproductHydrogenOptimizer_config.yaml`

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/natural_gas_supply_chain_optimization/run_byproduct_two_step.py \
  > byproduct_ng_two_step.log 2>&1 &
```

**输出目录**：`natural_gas_supply_chain_optimization/results/byproduct_hydrogen/`

**聚类文件**：`byproduct_clustering_results.json` (自动检测副产氢并重命名)

**注意**：此脚本不支持命令行参数，配置在代码和配置文件中预设。

---

### 12. 副产氢+绿氢两步法 | Byproduct H2 + Green H2 Two-Step

**场景说明**：结合副产氢和可再生能源制氢，两步法合成SAF

**配置文件**：`ByproductHydrogenSupplyChainOptimizer_config_two_step.yaml`

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/green_hydrogen_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process byproduct_two_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > byproduct_green_h2_two_step.log 2>&1 &
```

**输出目录**：`green_hydrogen_supply_chain_optimization/results/byproduct_hydrogen/two_step/`

**聚类文件**：`byproduct_clustering_results.json` (自动检测副产氢并重命名)

---

### 13. 副产氢+绿氢一步法 | Byproduct H2 + Green H2 One-Step

**场景说明**：结合副产氢和可再生能源制氢，一步法合成SAF

**配置文件**：`ByproductHydrogenSupplyChainOptimizer_config.yaml` (一步法)

```bash
nohup python /home/ljt/code_project/green_methanol_for_port_transportation-main/products/supply_chain_optimization/green_hydrogen_supply_chain_optimization/src/runner/unified_optimizer_runner.py \
  --process byproduct_one_step \
  --threads 8 \
  --time-limit 3600 \
  --mip-gap 0.01 \
  > byproduct_green_h2_one_step.log 2>&1 &
```

**输出目录**：`green_hydrogen_supply_chain_optimization/results/byproduct_hydrogen/one_step/`

**聚类文件**：`byproduct_clustering_results.json` (自动检测副产氢并重命名)

---

## 通用参数说明

### 支持命令行参数的模块

以下模块使用统一的`unified_optimizer_runner.py`，支持以下参数：

- **Coal Hydrogen** (煤制氢)
- **DAC Hydrogen** (DAC制氢)
- **Green Hydrogen** (绿氢)

#### 参数详解

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--process-type` 或 `--process` | `two_step` | 工艺类型选择 |
| `--threads` | `None` (自动) | CPU线程数，建议设置为物理核心数 |
| `--time-limit` | `3600` | 求解时间限制（秒），1小时 |
| `--mip-gap` | `0.01` | MIP间隙容差，1%表示接受1%的次优解 |
| `--log-level` | `INFO` | 日志级别 (DEBUG/INFO/WARNING/ERROR) |

#### 工艺类型选择值

| 模块 | 支持的 --process-type 值 |
|------|--------------------------|
| Coal Hydrogen | `two_step`, `one_step`, `byproduct_two_step` |
| DAC Hydrogen | `two_step`, `one_step`, `byproduct_two_step`, `byproduct_one_step` |
| Green Hydrogen | `two_step`, `one_step`, `byproduct_two_step`, `byproduct_one_step` |

### 不支持命令行参数的模块

以下模块使用独立的Python脚本，不支持命令行参数配置：

- **Natural Gas Two-Step**: `run_optimization.py`
- **Natural Gas One-Step**: `src/core/natural_gas_optimization_model_one_step.py`
- **Natural Gas Byproduct Two-Step**: `run_byproduct_two_step.py`

**注意**：天然气模块不包含副产氢一步法场景，因为一步法工艺不需要外部氢气输入，无法利用副产氢。

这些脚本的配置参数（如时间窗口、线程数等）需要在代码中直接修改。

---

## 常见问题

### Q1: 如何查看运行日志？

```bash
# 实时查看
tail -f <日志文件>

# 搜索关键字
grep "优化" <日志文件>

# 查看最后100行
tail -100 <日志文件>
```

### Q2: 如何终止正在运行的优化？

```bash
# 查找进程ID
ps aux | grep unified_optimizer_runner.py

# 终止进程
kill <进程ID>

# 强制终止
kill -9 <进程ID>
```

### Q3: Gurobi许可证错误怎么办？

确保许可证文件存在：
```bash
ls -la /home/ljt/gurobi.lic
```

检查环境变量：
```bash
echo $GRB_LICENSE_FILE
```

如果环境变量未设置，导出正确路径：
```bash
export GRB_LICENSE_FILE=/home/ljt/gurobi.lic
```

### Q4: 内存不足错误怎么办？

1. 减少时间窗口（修改代码中的`time_horizon_weeks`参数）
2. 增加系统交换空间
3. 使用更严格的MIP间隙（如 `--mip-gap 0.05`）
4. 减少并行线程数（如 `--threads 4`）

### Q5: 如何批量运行所有场景？

创建shell脚本 `run_all_scenarios.sh`：

```bash
#!/bin/bash

# 绿氢场景
nohup python .../coal_hydrogen_saf_optimization/src/runner/unified_optimizer_runner.py --process-type two_step > coal_two_step.log 2>&1 &
sleep 2

nohup python .../dac_hydrogen_saf_supply_chain_optimization/src/runner/unified_optimizer_runner.py --process two_step > dac_two_step.log 2>&1 &
sleep 2

# ... 添加其他场景

echo "所有优化任务已启动，请使用 ps aux | grep python 查看进程状态"
```

赋予执行权限并运行：
```bash
chmod +x run_all_scenarios.sh
./run_all_scenarios.sh
```

### Q6: 聚类文件命名规则是什么？

**自动检测和命名机制**：

所有模块（Coal、DAC、Green H2）的氢气聚类优化器都支持**自动检测副产氢数据**并重命名输出文件：

1. **检测规则**：
   - 检查聚类成员位置名称
   - 如果包含 `steel_`（钢铁厂）、`refinery_`（炼油厂）或 `byproduct`（副产）关键词
   - 自动判定为副产氢数据

2. **命名规则**：
   - **可再生能源氢气聚类**：`clustering_results.json`
   - **副产氢聚类**：`byproduct_clustering_results.json`

3. **配置参数**：
```yaml
clustering_parameters:
  enable_clustering: true
  export_clustering_results: true
  clustering_output_path: "clustering_results.json"  # 基础文件名
  # 如果检测到副产氢，自动重命名为 byproduct_clustering_results.json
```

**示例**：
```python
# 可再生能源数据 (solar_plant_1, wind_farm_2, ...)
→ 输出: clustering_results.json

# 副产氢数据 (steel_Angang, refinery_Sinopec, ...)
→ 输出: byproduct_clustering_results.json
```

**重要提示**：
- 不需要手动修改配置文件来区分副产氢和可再生能源
- 只需要在数据源中使用正确的位置命名规范
- 优化模型会自动加载对应的聚类文件

### Q7: 如何修改聚类参数？

聚类参数在配置文件 `shared/data/` 目录下：

- `ByproductHydrogenSupplyChainOptimizer_config.yaml`
- `CoalByproductHydrogenSAFOptimizer_config.yaml`
- `DACByproductHydrogenSAFOptimizer_config_two_step.yaml`
- 等

关键参数：
```yaml
clustering_parameters:
  enable_clustering: true
  eps_distance_km: 20.0        # DBSCAN聚类半径（公里）
  min_samples: 2               # 最小样本数
  max_clusters: 100            # 最大聚类数量
```

---

## 最近修复记录

### 2025-11-23 修复内容

**修复1：煤制氢模块绿氢与副产氢配置文件混淆**

**问题**：
- 运行 `--process-type two_step` 时期望使用绿氢（可再生能源），但实际使用了副产氢配置
- 配置文件映射错误，所有工艺类型都指向了 `CoalByproductHydrogenSAFOptimizer_config.yaml`
- 导致可视化结果显示负碳（来自老的配置），但新配置应该不计算负碳

**原因**：
- `unified_optimizer_runner.py` 的 `CONFIG_MAPPING` 字典错误
- 未区分绿氢场景和副产氢场景的配置文件

**解决方案**：
修改配置文件映射为：
```python
CONFIG_MAPPING = {
    'two_step': 'products/supply_chain_optimization/coal_hydrogen_saf_optimization/data/CoalHydrogenSAFOptimizer_config.yaml',  # 绿氢
    'one_step': 'products/supply_chain_optimization/coal_hydrogen_saf_optimization/data/CoalHydrogenSAFOptimizer_config.yaml',   # 绿氢
    'byproduct_two_step': 'shared/data/CoalByproductHydrogenSAFOptimizer_config.yaml',  # 副产氢
}
```

**配置文件区别**：
- `CoalHydrogenSAFOptimizer_config.yaml`（绿氢）：
  - 数据源：`solar_generation` 和 `3hourly_generation`（可再生能源）
  - 输出目录：`results/`
  - 聚类文件：`clustering_results.json`

- `CoalByproductHydrogenSAFOptimizer_config.yaml`（副产氢）：
  - 数据源：`industrial_byproduct_hydrogen` 和 `byproduct_h2_generation`
  - 输出目录：`results/byproduct_hydrogen/`
  - 聚类文件：`byproduct_clustering_results.json`

**影响场景**：
- 场景1：煤制氢+绿氢两步法 ✅ 现在使用正确的绿氢配置
- 场景8：副产氢+煤气化CO₂两步法 ✅ 继续使用副产氢配置

**修复文件**：
- `coal_hydrogen_saf_optimization/src/runner/unified_optimizer_runner.py`

**验证方法**：
运行绿氢场景后，检查：
1. 日志中显示加载的配置文件路径
2. 结果保存在 `results/` 目录（非 `results/byproduct_hydrogen/`）
3. 生成的聚类文件名为 `clustering_results.json`

---

**修复2：绿氢场景碳排放计算中的负碳问题**

**问题**：
- 可视化显示煤制氢场景有负碳（-46.12M kg），但配置明确禁用了负碳计算
- 实际是可视化读取了旧的结果文件（2025-11-20）

**原因**：
- 可视化脚本读取最新文件时，绿氢场景的最新结果还是11月20日的
- 那时配置可能还启用了负碳计算（或使用了错误的配置）

**解决方案**：
1. 修正配置文件映射（见修复1）
2. 重新运行绿氢场景生成新结果
3. 新结果会正确显示 `co2_utilization_credit: 0.0`

**碳核算说明**：
- 煤气化CO₂来自化石燃料（煤炭），属于化石碳
- 将化石碳循环利用制成SAF，不属于"碳移除"
- 符合国际碳核算标准（CORSIA），不应计入负排放
- 只有DAC（直接空气捕获）场景才有真正的负碳

---

**修复3：煤制氢模块配置文件错误**

**问题**：`coal_hydrogen_saf_optimization` 模块的 `unified_optimizer_runner.py` 使用了错误的配置文件路径，导致报错：
```
KeyError: "Configuration missing 'coal_parameters' section."
```

**原因**：配置文件映射错误地指向了绿氢配置文件（`GreenHydrogenSupplyChainOptimizer_config.yaml`），而不是煤制氢配置文件。

**解决方案**：
- 修改 `CONFIG_MAPPING` 字典，将所有工艺类型指向正确的配置文件
- 煤制氢模块所有工艺类型共用 `CoalByproductHydrogenSAFOptimizer_config.yaml`

**修复文件**：
- `coal_hydrogen_saf_optimization/src/runner/unified_optimizer_runner.py`

---

**修复2：氢气聚类方法参数不匹配**

**问题**：调用 `cluster_hydrogen_plants()` 方法时传入 `source_type` 参数，但方法签名不接受此参数，导致报错：
```
TypeError: HydrogenClusteringOptimizer.cluster_hydrogen_plants() got an unexpected keyword argument 'source_type'
```

**原因**：优化模型代码调用时传入了 `source_type='auto'` 参数，但氢气聚类优化器的方法定义中没有这个参数。

**解决方案**：
- 在 `cluster_hydrogen_plants()` 方法签名中添加 `source_type` 参数（默认值 `'auto'`）
- 添加详细的文档字符串说明参数用途
- 该参数暂时保留用于向后兼容，当前版本通过位置名称自动检测副产氢

**修复文件**：
- `coal_hydrogen_saf_optimization/src/hydrogen/hydrogen_clustering_optimizer.py`
- `dac_hydrogen_saf_supply_chain_optimization/src/hydrogen/hydrogen_clustering_optimizer.py`
- `green_hydrogen_supply_chain_optimization/src/hydrogen/hydrogen_clustering_optimizer.py`

---

**修复3：增强配置加载调试日志**

**改进**：在 `unified_optimizer_runner.py` 的配置文件加载过程中添加详细的调试日志：
```python
self.logger.info(f"[CONFIG] Process type: {process_type}")
self.logger.info(f"[CONFIG] Relative config path: {relative_config_path}")
self.logger.info(f"[CONFIG] Repo root: {repo_root}")
self.logger.info(f"[CONFIG] Full config path: {self.config_path}")
```

**好处**：方便追踪配置文件加载问题，快速定位路径错误。

---

## 联系方式

如有问题，请联系项目维护者或查阅项目文档。

**项目路径**：`/home/ljt/code_project/green_methanol_for_port_transportation-main/`

**文档创建日期**：2025-11-23
**最后更新日期**：2025-11-23 (修复煤制氢配置文件错误和氢气聚类参数不匹配问题)

---

**注意事项**：
1. 所有路径为绝对路径，确保在任何工作目录下都能正确执行
2. 日志文件保存在当前工作目录，建议创建专门的日志目录
3. 部分场景运行时间可能超过1小时，建议适当调整 `--time-limit` 参数
4. 副产氢场景需要先运行相应的可再生能源场景生成基础数据
